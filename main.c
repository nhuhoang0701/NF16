#include "tp3.h"
#include <stdio.h>

int main(int argc, char const* argv[])
{
    while (1) {
        int choix;
        printf("Choisissez votre demande: ");
        scanf("%d", choix);
        if (choix == -1) {
            break;
        }
        switch (choix)
        {
        case 1:
            break;
        
        default:
            break;
        }

    }
    return 0;
}
